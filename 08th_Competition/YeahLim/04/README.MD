# [프로그래머스 레벨 2] [1차] 프렌즈4블록

## ?  **time**
40분

## :pushpin: **Algorithm**
구현

## ??**Time Complexity**
$O(N^2)$

## :round_pushpin: **Logic**


## :black_nib: **Review**   
- 블록 아래부터 다시채울 때 `queue`나 `stack`사용
- i, j / m, n 배열 위치 조심

## :black_nib: **C++**
- cpp에서 전역변수 사용할때 `::board = board`
- cpp에서 string은 `string[2]` 이런식으로 `[]`연산자 사용 가능
- java -> cpp 로 대응
    - `Queue` -> `queue`
    - `LinkedList` -> `list`
    - `ArrayList` -> `vector`
    - `Stack` -> `stack`
    - `PriorityQueue` -> `priority_queue`